# Supabase configuration removed
# NOTE: Supabase integration was intentionally removed from this repository.
# The original Supabase configuration and migration files were neutralized to
# prevent accidental use. If you prefer, delete the `supabase/` directory.